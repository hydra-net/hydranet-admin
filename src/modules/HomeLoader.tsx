import { Container } from "./Home/shared";
import LoadingSpinner from "../common/components/LoadingSpinner";

const HomeLoader = () => {
  return (
    <Container>
      <LoadingSpinner />
    </Container>
  );
};

export default HomeLoader;
